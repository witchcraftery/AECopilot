// settings.js
document.addEventListener("DOMContentLoaded", function () {
  // The settings page now only provides information about the .env file
  // No interactive elements are needed as the API key is configured via environment variables

  console.log("Settings page loaded - API key is configured via .env file");
});
